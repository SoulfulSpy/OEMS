spring.application.name=backend
server.port=8080

# DataSource configuration with environment overrides
# If SPRING_DATASOURCE_URL is set, it takes precedence
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://pg-3ce98716-oemsnaiyo24.j.aivencloud.com:16965/defaultdb?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:avnadmin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:AVNS_Q1uEOB1I4X02uFpbeq8}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.default_schema=public

# CORS allowed origins (frontend dev server); can be overridden by env
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173}
